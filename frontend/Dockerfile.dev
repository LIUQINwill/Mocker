# 前端开发环境 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 设置npm镜像源（提高国内访问速度）
RUN npm config set registry https://registry.npmmirror.com/

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发环境启动命令
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]